# PetroChina ROIC official-fact readiness audit (FY2020–FY2025)

- Formula candidate: `B_operating_profit_bridge_financing_view`
- Evidence gate: **BLOCKED_WITH_EXPLICIT_GAPS**
- Shadow: **NOT_RUN** — the direct operating-tax, scope-matched finance and non-operating asset evidence gate is not satisfied
- Canonical inventory facts audited: `64`

The matrix is an evidence gate, not a ROIC calculation. Missing values are not replaced with zero, and no production Metric Result or value profile is written.

## Coverage by year

| FY | Ready | Missing | N/A |
|---:|---:|---:|---:|
| 2020 | 2 | 18 | 12 |
| 2021 | 11 | 21 | 0 |
| 2022 | 11 | 21 | 0 |
| 2023 | 11 | 21 | 0 |
| 2024 | 11 | 21 | 0 |
| 2025 | 11 | 21 | 0 |

## Blocking gaps

- FY2021 `finance_cost`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2022 `finance_cost`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2023 `finance_cost`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2024 `finance_cost`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2025 `finance_cost`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2021 `interest_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2022 `interest_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2023 `interest_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2024 `interest_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2025 `interest_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2021 `lease_interest_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2022 `lease_interest_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2023 `lease_interest_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2024 `lease_interest_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2025 `lease_interest_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2021 `investment_income`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2022 `investment_income`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2023 `investment_income`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2024 `investment_income`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2025 `investment_income`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2021 `fair_value_change`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2022 `fair_value_change`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2023 `fair_value_change`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2024 `fair_value_change`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2025 `fair_value_change`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2021 `other_non_operating_income`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2022 `other_non_operating_income`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2023 `other_non_operating_income`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2024 `other_non_operating_income`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2025 `other_non_operating_income`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2021 `other_non_operating_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2022 `other_non_operating_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2023 `other_non_operating_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2024 `other_non_operating_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2025 `other_non_operating_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2021 `operating_tax_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2022 `operating_tax_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2023 `operating_tax_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2024 `operating_tax_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2025 `operating_tax_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2021 `income_tax_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2022 `income_tax_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2023 `income_tax_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2024 `income_tax_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2025 `income_tax_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2021 `deferred_tax_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2022 `deferred_tax_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2023 `deferred_tax_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2024 `deferred_tax_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2025 `deferred_tax_expense`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2021 `profit_before_tax`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2022 `profit_before_tax`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2023 `profit_before_tax`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2024 `profit_before_tax`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2025 `profit_before_tax`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2020 `nci`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2021 `nci`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2022 `nci`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2023 `nci`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2024 `nci`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2025 `nci`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2020 `short_term_borrowings`: required input is absent from the canonical eligible Fact inventory (missing_canonical_fact)
- FY2020 `current_portion_of_long_term_borrowings`: required input is absent from the canonical eligible Fact inventory (missing_canonical_fact)
- FY2020 `current_portion_of_bonds_payable`: required input is absent from the canonical eligible Fact inventory (missing_canonical_fact)
- FY2020 `current_portion_of_lease_liabilities`: required input is absent from the canonical eligible Fact inventory (missing_canonical_fact)
- FY2020 `long_term_borrowings`: required input is absent from the canonical eligible Fact inventory (missing_canonical_fact)
- FY2020 `bonds_payable`: required input is absent from the canonical eligible Fact inventory (missing_canonical_fact)
- FY2020 `lease_liabilities`: required input is absent from the canonical eligible Fact inventory (missing_canonical_fact)
- FY2020 `interest_bearing_long_term_payables`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2021 `interest_bearing_long_term_payables`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2022 `interest_bearing_long_term_payables`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2023 `interest_bearing_long_term_payables`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2024 `interest_bearing_long_term_payables`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2025 `interest_bearing_long_term_payables`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2020 `cash_and_cash_equivalents`: required input is absent from the canonical eligible Fact inventory (missing_canonical_fact)
- FY2020 `monetary_funds`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2021 `monetary_funds`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2022 `monetary_funds`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2023 `monetary_funds`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2024 `monetary_funds`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2025 `monetary_funds`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2020 `restricted_cash`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2021 `restricted_cash`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2022 `restricted_cash`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2023 `restricted_cash`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2024 `restricted_cash`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2025 `restricted_cash`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2020 `financial_assets_at_fair_value`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2021 `financial_assets_at_fair_value`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2022 `financial_assets_at_fair_value`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2023 `financial_assets_at_fair_value`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2024 `financial_assets_at_fair_value`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2025 `financial_assets_at_fair_value`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2020 `financial_assets_at_amortized_cost`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2021 `financial_assets_at_amortized_cost`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2022 `financial_assets_at_amortized_cost`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2023 `financial_assets_at_amortized_cost`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2024 `financial_assets_at_amortized_cost`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2025 `financial_assets_at_amortized_cost`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2020 `associate_investment`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2021 `associate_investment`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2022 `associate_investment`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2023 `associate_investment`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2024 `associate_investment`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2025 `associate_investment`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2020 `joint_venture_investment`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2021 `joint_venture_investment`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2022 `joint_venture_investment`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2023 `joint_venture_investment`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2024 `joint_venture_investment`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2025 `joint_venture_investment`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2020 `goodwill`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2021 `goodwill`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2022 `goodwill`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2023 `goodwill`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2024 `goodwill`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2025 `goodwill`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2020 `noninterest_bearing_operating_liabilities`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2021 `noninterest_bearing_operating_liabilities`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2022 `noninterest_bearing_operating_liabilities`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2023 `noninterest_bearing_operating_liabilities`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2024 `noninterest_bearing_operating_liabilities`: required input is absent from the canonical eligible Fact inventory (missing)
- FY2025 `noninterest_bearing_operating_liabilities`: required input is absent from the canonical eligible Fact inventory (missing)

## Frozen controls

- NOPAT cannot be net profit or parent-attributable profit.
- Invested capital uses opening and closing balances; ending balance alone is insufficient.
- Cash is classified by purpose; all cash is never silently subtracted.
- PIT uses maximum `available_at`; restatements supersede earlier canonical facts.

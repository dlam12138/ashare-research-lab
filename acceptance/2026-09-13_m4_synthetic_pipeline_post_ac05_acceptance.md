# Acceptance: M4 synthetic pipeline after AC-05 correction

Verdict: **PASS** for this synthetic-only implementation stage. Goal: [post-AC-05 contract](../agent/goals/2026-09-13_m4_synthetic_pipeline_post_ac05_acceptance.md); [work record](../agent/record/2026-09-13_01_m4-synthetic-pipeline-post-ac05-acceptance.md).

The verified base is `main@c9ff0c5a17b022ddbce0bd724e10a5ff6aaafb51`, including the normal merge of AC-05 correction PR #18 after 42 successful checks. The branch is `codex/m4-synthetic-pipeline-acceptance`. The transferred `pipeline/__init__.py` and `pipeline/orchestrator.py` are byte-identical to the clean preserved candidate `f5f419e`; old `CHANGES_REQUIRED` records were not imported or treated as a pass. The only test changes update the two now-stale normative blob hashes, make AC-05's corrected S6 error explicit, and give AC-21 an isolated pytest temp directory while retaining all its no-I/O assertions. README is updated to link this acceptance evidence and state only synthetic, in-memory capability.

Direct code/test review found the frozen 20-symbol export surface, 14 pipeline errors, S0–S8/G1–G14/V1–V6/L1–L14 composition, immutable canonical serialization, read-only registry binding, bounded calls, rank/quality gates and authorization restrictions intact. AC-01–AC-30 executed without skips or xfails. AC-05 tests both empty-domain `AdapterError("EMPTY_EXPECTED_DOMAIN")` and three-row S6 `ExecutionError("SINGULAR_DESIGN")`; no partial envelope or rank-gate bypass is accepted. The pipeline package introduces no provider, database, filesystem, network, holdout or real-research execution path.

The first full pipeline run had one failure caused by AC-21's shared-temp-directory snapshot; an unchanged full rerun passed, then AC-21 was tightened to a dedicated `tmp_path` under a committed Goal addendum. The final run gave **51 passed** (exit 0). Seven upstream suites gave **328 passed** (exit 0). The initial governance run failed because this file had not yet been created; after creation it gave **15 passed** (exit 0). Ruff passed (exit 0). Initial failures are retained as failures in the record. Exact commands and times are in the linked work record.

Changed paths are restricted to the Goal, two new source files, one new test, README, this acceptance file and its work record. Normative docs and all other protected paths remain at the verified main blobs. The original M2 dirty files, one stash and default database SHA256 were preserved; the prior candidate worktree remains clean. Main/local origin/live synchronization and post-commit worktree status are checked separately in the final evidence packet.

This acceptance covers caller-supplied synthetic strategy inputs only. It authorizes no real data, holdout, research conclusion, ranking, recommendation, trading, or following project stage.

# Claude Code Project Instructions

@agent/agent.md

## Session Startup

每次进入本项目并开始新任务时，必须先执行以下步骤：

1. 阅读本文件加载的 `agent/agent.md`。
2. 阅读 `agent/record/README.md`。
3. 检查当前 Git 状态、分支和提交。
4. 查看 `agent/record/` 中最近修改的三份工作记录。
5. 搜索并阅读与当前任务名称或模块相关的历史记录。
6. 根据记录确认：
   - 当前项目进度；
   - 已完成内容；
   - 未完成内容；
   - 已有工程决策；
   - 已知风险；
   - 禁止重复开展的工作。
7. 在修改任何项目文件前，为本次任务创建或更新工作记录。
8. 后续工作严格遵守 `agent/agent.md`。

## Record Reading Scope

不要在每次任务开始时读取全部历史记录。

默认只读取：

- `agent/record/README.md`；
- 最近修改的三份记录；
- 与当前任务关键词直接相关的记录。

只有在当前任务需要完整历史背景时，才扩大读取范围。

## Record Update Requirement

任何会修改项目文件的任务，都必须：

1. 在 `agent/record/` 创建或更新对应工作记录；
2. 在记录中写明任务目标、范围、非目标和开始状态；
3. 工作过程中持续记录实际修改和实际执行的验证；
4. 任务结束前更新结果、遗留问题和 Git 状态。

禁止在任务全部完成后凭记忆一次性虚构工作过程。

## Honesty Requirement

- 没有读取过的文件，不得声称已经读取；
- 没有执行过的命令，不得写入记录；
- 没有通过的测试，不得声称通过；
- 计划、推测和实际结果必须明确区分；
- 无法确认的信息必须标为未知或假设。

## Scope Reminder

本项目只围绕以下两个核心模块开发：

1. 个股价值评估；
2. 市场机制验证。

免费数据底座、数据质量、报告和本地界面只能作为以上模块的支持能力。

当前不开发：

- 自动交易；
- 券商下单；
- 明日涨跌预测；
- 黑箱荐股；
- 精确目标价；
- 全市场长期分钟数据库；
- 与核心目标无关的大型功能。

## Claude Code Verification

新会话开始后，应使用 Claude Code 的上下文检查能力确认：

- 根目录 `CLAUDE.md` 已加载；
- `agent/agent.md` 已通过导入加载。

如果导入未生效，应先报告问题，不得假装已经加载。
